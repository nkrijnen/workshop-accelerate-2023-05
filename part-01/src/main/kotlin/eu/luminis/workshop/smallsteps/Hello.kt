package eu.luminis.workshop.smallsteps

// First run the test suite and check that it fails, then change the code below, so it passes:
//
// fun hello(name: String) = "Hello $name"
//
fun hello(name: String) = "Hello $name"